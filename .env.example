# 说明：
# - `.env.example` 是环境变量模板（可以提交到 git）
# - 复制为 `.env` 后再按需修改（`.env` 已在 `.gitignore` 中忽略）
# - 下面同时包含后端（acm-site）和前端（fronted-acm）需要的环境变量

# ======================== 后端（acm-site） ========================
# MySQL 数据库配置
# 数据库地址（本机一般是 127.0.0.1）
DB_HOST=127.0.0.1
# 数据库端口
DB_PORT=3306
# 数据库用户名
DB_USER=root
# 数据库密码
DB_PASSWORD=your_password_here
# 数据库名称
DB_NAME=acm_site
# 字符集（推荐 utf8mb4）
DB_CHARSET=utf8mb4
# 连接池：最大空闲连接数
DB_MAX_IDLE_CONNS=10
# 连接池：最大打开连接数
DB_MAX_OPEN_CONNS=100

# JWT 配置
# JWT 签名密钥（生产环境必须修改）
JWT_SECRET_KEY=change_me_in_production
# Token 过期时间（小时）
JWT_EXPIRATION_HOURS=24

# 服务监听配置
# 后端监听端口（默认 8080）
SERVER_PORT=8080
# 可选：监听地址（默认 127.0.0.1，仅本机访问）
# SERVER_HOST=127.0.0.1
# Gin 运行模式：debug / release
GIN_MODE=debug

# CORS 跨域配置
# 允许的前端来源（逗号分隔）；本地开发默认 5173
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ======================== 前端（fronted-acm / Vite） ========================
# 后端 API 前缀（建议以 /api 结尾）
VITE_API_BASE_URL=http://127.0.0.1:8080/api

# Token 自动刷新间隔（毫秒）：3000000 = 50 分钟
VITE_TOKEN_REFRESH_INTERVAL=3000000
# localStorage 存储 token 的 key
VITE_TOKEN_KEY=auth_token
# localStorage 存储用户信息的 key
VITE_USER_INFO_KEY=user_info

# 外部链接（用于页面跳转）
VITE_QQ_GROUP_URL=https://jq.qq.com/?_wv=1027&k=KoVRvLvL
VITE_BILIBILI_URL=https://space.bilibili.com/3546651937475184?spm_id_from=333.337.search-card.all.click
VITE_OJ_URL=https://hydro.ac/d/cdjcc_acm_2333/

# 默认头像（用户未设置头像时使用）
VITE_DEFAULT_AVATAR=https://cube.elemecdn.com/3/7c/3ea6beec64369c2642b92c6726f1epng.png
# 应用名称（页面标题等）
VITE_APP_NAME=CDJCC ACM 集训队官网
